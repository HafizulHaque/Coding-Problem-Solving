#include <bits/stdc++.h>
using namespace std;


int main(){
    long long n , res, temp;
    while(cin>>n){
        temp = n;
        res = n;

        while(temp)

        if(res>n) cout << res << endl;
        else cout << n << endl;
    }
    return 0;
}
