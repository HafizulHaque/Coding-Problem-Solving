#include <bits/stdc++.h>
using namespace std;

int main(){
    string hill;
    cin>>hill;
    pair<int, int>pi;
    stack<int>st;
    int len = hill.length();
    int arr[len];
    memset(arr, 0, sizeof(arr));
    for(int i = 0; i < len; ++i){
        if()
    }
}
